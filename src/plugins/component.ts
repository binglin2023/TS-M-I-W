import { loadAsyncComponent } from "../utils/async-compotent";

export default {
    install( app:{ component: (arg0: string, arg1: any) => void }){
        // app.component{
        //     `Bl_query`,
        //     loadAsyncComponent(() => import("@/components/query.vue"))
        // };
        
    } 
}